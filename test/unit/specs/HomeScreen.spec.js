import Vue from 'vue'
import HomeScreen from '@/devmode/HomeScreen'

describe('HomeScreen.vue', () => {

  it('has name configured', () => {
    expect(HomeScreen.name).toEqual('home-screen')
  })
  
})
